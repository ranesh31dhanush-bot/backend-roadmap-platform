{
  "name": "top1-backend-roadmap-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Top 1% Backend Developer Roadmap - Dynamic Enterprise Learning Platform",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "start": "node backend/dist/server.js",
    "build": "npm run build:shared && npm run build:backend",
    "build:all": "npm run build --workspaces --if-present",
    "build:shared": "npm run build --workspace=shared",
    "build:backend": "npm run build --workspace=backend",
    "build:frontend": "npm run build --workspace=frontend",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:unit": "npm run test:unit --workspaces --if-present",
    "test:integration": "npm run test:integration --workspace=backend --if-present",
    "test:e2e": "playwright test",
    "clean": "npm run clean --workspaces --if-present"
  },
  "dependencies": {
    "@types/node": "^22.13.1",
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "prettier": "^3.4.2"
  }
}
